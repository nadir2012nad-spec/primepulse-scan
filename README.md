# PrimePulse OPS Client Scan — Conversion V3

This package keeps the original generator and Vercel routing, but upgrades the real client scan page served by `/scan/:chain/:id` through `api/scan.js`.

V3 changes:
- Desktop above-the-fold issue fixed with visible recommendation categories and scroll cue
- Hero made more compact so the user immediately sees there is a full report below
- Added recommended recovery path, founder decision window, action categories, and loss/risk anchors
- Added stronger psychological pressure sections without breaking token image OG preview
- OG/Twitter preview remains token-image focused via DexScreener image URL

Upload this package to Vercel and redeploy.
